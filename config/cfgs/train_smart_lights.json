{
    "max_epochs": 40,
    "batch_size": 64,
    "es_patience": 10,
    "es_criterion": "val_loss",
    "es_lower_is_better": true,
    "optimizer": [
        "Adam",
        {
            "lr": 5e-3
        }
    ]
}
